# Ubuntu-Network-Scripts
Ubuntu Network Scripts

A series of scripts (2) to be able to type in the commands IP or DHCP to be able to switch between a static IP or a DHCP assigned IP address. Be sure to change the IP address in 

Setup:
Copy scripts to /etc/netplan


